# Windows-specific requirements to avoid compilation issues
# Use this file if you encounter compilation problems with the main requirements.txt

discord.py==2.3.2
python-dotenv==1.0.0
# Use newer OpenAI client that supports Responses + Conversations
openai>=1.99.9
asyncio-mqtt==0.16.1
aiohttp==3.9.1
# Use pure Python alternatives
msgspec==0.19.0
httpx==0.25.2
typing-extensions==4.8.0

# Alternative approach: if you still have issues, try these pure Python packages
# openai>=1.99.9  # Newer version with Responses/Conversations support
# pydantic>=2.0.0  # Only if you need it directly
